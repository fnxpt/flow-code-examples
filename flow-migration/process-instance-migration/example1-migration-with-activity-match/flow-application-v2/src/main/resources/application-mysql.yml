# This enables the use of MySQL

spring.datasource.url: jdbc:mysql://${FLOW_MYSQL_HOST:localhost}:${FLOW_MYSQL_PORT:3306}/${FLOW_MYSQL_DATABASE_NAME:send-personal-message-flow}
spring.datasource.username: root
spring.datasource.password: root